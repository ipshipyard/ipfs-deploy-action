name: Release

on:
  push:
    paths: [CHANGELOG.md]
    branches: [main]
  pull_request:
    paths: [CHANGELOG.md]
    branches: [main]

permissions:
  contents: write

jobs:
  release:
    uses: ipdxco/unified-github-workflows/.github/workflows/release.yml@v1.0
